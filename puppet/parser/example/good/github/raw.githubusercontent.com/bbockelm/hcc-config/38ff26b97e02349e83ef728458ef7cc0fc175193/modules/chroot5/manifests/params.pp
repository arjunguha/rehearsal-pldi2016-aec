class chroot5::params {

   $chroot_top  = "/chroot"

   $chroot_version = "sl5-v5"

   $chroot_base = "${chroot_top}/${chroot_version}"

	$chroot_root = "${chroot_base}/root"

   $chroot_user = "chroot_creator"

}
